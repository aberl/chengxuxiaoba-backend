spring:
  profiles:
    active: prod
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    url: jdbc:mysql://localhost:3306/chengxuxiaoba?serverTimezone=Asia/Shanghai
    username: root
    password: 1qaz@WSX
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
      #   数据源其他配置
    initialSize: 5
    minIdle: 5
    maxActive: 20
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    #   配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙
    filters: stat,wall,log4j
    maxPoolPreparedStatementPerConnectionSize: 20
    useGlobalDataSourceStat: true
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500

uploadfile:
  savepath: D:/peoject/chengxuxiaoba/files
  size: 10M
  accesshostname: http://localhost:7080/chengxuxiaoba

ali:
  accessKey:
    RAM:
      accesskeyid: LTAI4FwSm1WwZ4ZoHca2BcAB
      AccessKeySecret: fe8ZZgw03v0YLZ1iddsHkY05jaf3Tw
      normalRoleArn: acs:ram::1165752204112719:role/chengxuxiaoba-user
  video:
    encryptionTemplateId: 1a4d2c29f76f55670dd6f7a6d3b24cce
    nontranscodeTemplateId: VOD_NO_TRANSCODE
  kms:
    serviceKey: cf1521c1-f1d5-48b8-b8ed-47114de24a8d
  hls:
    decryptUrl: http://localhost:9080/api/hls/decrypt