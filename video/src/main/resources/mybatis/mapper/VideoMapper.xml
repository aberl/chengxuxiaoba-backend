<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="com.chengxuxiaoba.video.mapper.VideoMapper">
        <insert id="insert" parameterType="com.chengxuxiaoba.video.model.po.Video">
            <selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="id">
                SELECT LAST_INSERT_ID()
            </selectKey>
            INSERT INTO Video(courseModuleId, file, name,attachments, duration, description, images, status)
            VALUES(#{courseModuleId},#{file},#{name},#{attachments}, #{duration}, #{description}, #{images},#{status})
        </insert>

    <select id="getListWithPage" parameterType="com.chengxuxiaoba.video.model.query.VideoQuery" resultType="com.chengxuxiaoba.video.model.po.Video">
        SELECT * from Video
        where courseModuleId=#{courseModuleId}
    </select>

    <select id="getVideo" parameterType="int" resultType="com.chengxuxiaoba.video.model.po.Video">
        SELECT * from Video
        where id=#{id} limit 1
    </select>

    <select id="getVideoByCourseModuleId" resultType="com.chengxuxiaoba.video.model.po.Video">
        SELECT * from Video
        where courseModuleId = #{courseModuleId}
    </select>

    <select id="getVideoByVideoName" resultType="com.chengxuxiaoba.video.model.po.Video">
        SELECT * from Video
        where courseModuleId = #{courseModuleId} and name=#{name} limit 1
    </select>

    <select id="getVideoSummary" parameterType="int" resultType="com.chengxuxiaoba.video.model.po.VideoSummary">
        SELECT courseModuleId,count(1) as videoCount,sum(viewCount) as totalViewCount,sum(praiseCount) as totalPraiseCount from Video
        where courseModuleId = #{courseModuleId}
    </select>

    <update id="updateVideo">
        UPDATE Video
        <trim prefix="set" suffixOverrides=",">
            <if test="courseModuleId != null">courseModuleId=#{courseModuleId},</if>
            <if test="file != null">file=#{file},</if>
            <if test="name != null">name=#{name},</if>
            <if test="attachments != null">attachments=#{attachments},</if>
            <if test="duration != null">duration=#{duration},</if>
            <if test="description != null">description=#{description},</if>
            <if test="images != null">images=#{images},</if>
            <if test="viewCount != null">viewCount=#{viewCount},</if>
            <if test="praiseCount != null">praiseCount=#{praiseCount},</if>
            <if test="status != null">status=#{status},</if>
        </trim>
        WHERE id=#{id}
    </update>
</mapper>